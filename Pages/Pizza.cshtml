@page
@model PizzaProjuct.Pages.PizzaModel
@{
}
    
<div class="container" style="background-color:lightblue; width:100% ; height:300px">
    <div class="row">
        <div class="col-6 mb-1">
        <img src="~/css/images/Pizzas/Create.png" class="w-50" >
        </div>
        <div class="col-6 d-flex flex-column justify-content-center align-items-center">
        <h1>Get your customized Pizza</h1>
        <a class="btn btn-primary" asp-area="" asp-page="Forms/CustomPizza">Try Now!</a>
        </div>
    </div>
</div>
<div class="container mt-3">
    <div class="row mt-1">
       
            @foreach (var item in Model.pizzasFakeDB)
            {
            var imagePath = "/css/images/Pizzas/"+(item.ImageTitle+".png");
            <div class="col-3">
                <img src="@imagePath" class="w-100 mt-2">
             @*    <a class="btn btn-primary" asp-area="" asp-page="Forms/CustomPizza">
                Get this Pizza!
                </a>*@
            </div>
            }
            
    </div>
</div>